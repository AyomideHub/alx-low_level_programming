#include "main.h"
/**
* _abs - funtion to computes the absolute value of an integer
* @n: parameter to be check
* Return: the absolute value of int
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
